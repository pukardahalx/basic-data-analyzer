# 📊 Basic Data Analyzer

A simple Python tool to analyze earthquake, temperature, and school exam data.

## What It Does
- Loads earthquake, temperature, and exam data from CSV files
- Calculates key statistics (averages, totals, maximums)
- Creates charts (bar graphs, line plots)
- Saves results as images and CSV files
- Generates a summary report

## Quick Start

### 1. Setup
```bash
# Clone repository
git clone https://github.com/pukardahalx/basic-data-analyzer.git
cd nepalese-data-analyzer

# Create virtual environment
python -m venv venv

# Activate (Windows)
venv\Scripts\activate

# Activate (Mac/Linux)
source venv/bin/activate

# Install packages
pip install pandas matplotlib
